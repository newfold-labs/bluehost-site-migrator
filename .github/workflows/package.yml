name: Package Plugin

on:
  release:
    types:
      - created
      - updated

jobs:
  build:
    name: Run
    runs-on: ubuntu-latest
    steps:

      - name: Checkout
        uses: actions/checkout@v2

      - name: PHP version
        run: php --version

      - name: Set Node.js 12.x
        uses: actions/setup-node@v1
        with:
          node-version: 12.x

      - name: Node version
        run: node --version

      - name: NPM version
        run: npm --version

      - name: npm install
        run: npm install

      - name: Build JavaScript
        run: npm run build

      - name: Generate .pot file
        run: npm run makepot

      - name: Validate composer.json and composer.lock
        run: composer validate

      - name: Install PHP Dependencies
        run: composer install --prefer-dist --no-progress --no-suggest --no-dev

      - name: Create Zip
        run: zip -r bluehost-move.zip . --exclude @./.distignore

      - name: List Files
        run: ls -lah

      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: https://uploads.github.com/repos/${{ github.repository }}/releases/${{ github.event.release.id }}/assets{?name,label}
          asset_path: ./bluehost-move.zip
          asset_name: bluehost-move.zip
          asset_content_type: application/zip
